# Beads Archive Marker

Archived on 2026-06-08 during `atelier-z1p.4`.

The current Beads export was imported with:

```bash
atelier import-beads .beads/issues.manual.jsonl --output .atelier-state
```

After cutover, `.beads/` is a read-only fallback. Normal repository tracker
commands use Atelier and committed `.atelier-state/`.
