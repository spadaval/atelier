# Retired `atelier mission`

`atelier mission` is removed. A mission is a typed issue record, not a parallel
command namespace.

Create and manage mission records through `atelier issue`; inspect their
detail with `atelier issue show <mission-id>`; and coordinate their work with
`atelier work mission <mission-id>`. Lifecycle changes use
`atelier issue transition <mission-id>`.
