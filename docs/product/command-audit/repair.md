# Retired `atelier repair`

`atelier repair` is removed. Its hidden active-work cleanup model is no longer
part of Atelier.

Use `atelier check --fix` only for ignored local runtime or projection repair.
Use normal issue transitions for durable workflow state.
