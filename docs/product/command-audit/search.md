# Retired `atelier search`

`atelier search` is removed pending a stronger cross-record search design. It
is not a current orientation or workflow command.

Use `atelier issue list` for current inventory and `atelier work ready`,
`atelier work blocked`, or scoped work dashboards for operational selection.
