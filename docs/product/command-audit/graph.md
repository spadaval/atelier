# Retired `atelier graph`

`atelier graph` is removed. Its hierarchy, blocker, and downstream-impact
questions belong to the record being inspected, not to an abstract namespace.

Use `atelier issue show <id>` for issue relationships and impact. For a
mission's coordinated work, use `atelier issue show <mission-id>` or
`atelier work mission <mission-id>`.
