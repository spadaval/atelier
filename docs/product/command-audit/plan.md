# Deferred `atelier plan`

`atelier plan` has no current command contract. First-class plan records and
plan CRUD are deferred.

Keep durable plan prose in ordinary Markdown referenced by accountable work or
evidence. For one-shot reviewed work-graph creation, use `atelier bundle`.
