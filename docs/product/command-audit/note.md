# Retired `atelier note`

`atelier note` is removed. Notes belong to their durable record rather than a
generic root surface.

Use `atelier issue note <id> "..."` for issue, mission, and handoff context.
