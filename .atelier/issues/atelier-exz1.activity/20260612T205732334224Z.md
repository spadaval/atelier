---
schema: "atelier.activity"
schema_version: 1
id: "20260612T205732334224Z"
subject_kind: "issue"
subject_id: "atelier-exz1"
event_type: "note"
actor: "root"
created_at: "2026-06-12T20:57:32.334224Z"
summary: "Added note"
---

Issue command surface classification, captured 2026-06-12.

Inventory sources:
- `atelier issue --help` lists 23 visible issue subcommands plus built-in `help`: create, quick, subissue, list, search, show, update, close, close-all, reopen, delete, comment, label, unlabel, block, unblock, blocked, relate, unrelate, related, impact, next, tree, tested.
- `src/main.rs` `IssueCommands` defines the same 23 product variants. No hidden issue variants or extra issue aliases were found in the enum.
- Current folded replacement flags present today: `issue create --parent`, `issue create --work`, `issue update --status open`, `issue update --label`, and `issue list --ready`.
- Current folded replacement gaps: `issue update --remove-label` and `issue list --blocked` do not exist yet.

Reduced target for primary `atelier issue --help`: `create`, `list`, `show`, `update`, `close`, plus built-in `help` only. Do not add visible compatibility aliases unless a human explicitly asks for a migration window; old paths that remain during any approved window must be hidden from primary help and print replacement guidance.

| Current issue subcommand | Source behavior today | Classification | Target home or replacement | Implementation owner |
| --- | --- | --- | --- | --- |
| `create` | lifecycle create; supports title, description, priority, template, label, issue-type, parent, work | keep | `atelier issue create` | keep in `atelier-64ea` tests |
| `list` | lifecycle list; filters status, label, priority, ready | keep | `atelier issue list`; add `--blocked` for blocked-list fold | `atelier-64ea` |
| `show` | issue detail read | keep | `atelier issue show <id>` | keep in help tests |
| `update` | lifecycle metadata update; supports title, description, priority, status, issue-type, label, parent/no-parent, claim, append-notes | keep | `atelier issue update <id>`; add `--remove-label <label>` | `atelier-64ea` |
| `close` | lifecycle close with optional reason | keep | `atelier issue close <id> --reason <reason>` | keep in help tests |
| `help` | clap built-in help command | keep | implicit `atelier issue help <command>` | no product work |
| `quick` | creates issue with `work: true` | fold into lifecycle options | `atelier issue create <title> --work [--label <label>]` | `atelier-64ea` |
| `subissue` | creates issue with parent and optional work flag | fold into lifecycle options | `atelier issue create <title> --parent <id> [--work]` | `atelier-64ea` |
| `reopen` | calls reopen lifecycle helper | fold into lifecycle options | `atelier issue update <id> --status open` | `atelier-64ea` |
| `label` | adds one label via canonical label mutation | fold into lifecycle options | `atelier issue update <id> --label <label>` | `atelier-64ea` |
| `unlabel` | removes one label via canonical label mutation | fold into lifecycle options | `atelier issue update <id> --remove-label <label>`; flag is missing today | `atelier-64ea` |
| `blocked` | lists blocked issues through dependency projection | fold into lifecycle list option | `atelier issue list --blocked`; flag is missing today | `atelier-64ea` |
| `block` | adds blocking dependency | move to domain command group | `atelier dep add <blocked> <blocker>`; top-level `dep add` exists | `atelier-drfm` |
| `unblock` | removes blocking dependency | move to domain command group | `atelier dep remove <blocked> <blocker>`; top-level `dep remove` exists | `atelier-drfm` |
| `search` | issue-only text search | move to cross-record search | `atelier search <query>` with issue filtering when needed; group is missing today | `atelier-drfm` |
| `relate` | adds typed issue relation | move to typed link group | `atelier link add issue <id> issue <related> --type <type>`; group is missing today | `atelier-drfm` |
| `unrelate` | removes typed issue relation | move to typed link group | `atelier link remove issue <id> issue <related> --type <type>`; group is missing today | `atelier-drfm` |
| `related` | lists related issues | move to typed link group | `atelier link list issue <id>`; group is missing today | `atelier-drfm` |
| `impact` | downstream impact over hierarchy and impact-bearing links | move to graph/hierarchy view | `atelier graph impact <id>`; group is missing today | `atelier-drfm` |
| `tree` | full or compact hierarchy view | move to graph/hierarchy view | `atelier graph tree [--status <status>] [--compact]`; group is missing today | `atelier-drfm` |
| `comment` | adds activity/comment sidecar with kind | move to activity/note group | `atelier note add issue <id> "..." --kind <kind>`; group is missing today | `atelier-drfm` |
| `delete` | destructive issue delete with force flag | move to maintenance/destructive group | `atelier maintenance delete issue <id> [--force]`; group is missing today | `atelier-drfm` |
| `close-all` | bulk close by label or priority | remove | no normal replacement; reconsider only as explicit maintenance bulk command under a separate issue if needed | `atelier-drfm` |
| `next` | suggests next issue | remove | use `atelier status`, `atelier mission status <id>`, or `atelier issue list --ready` | `atelier-drfm` |
| `tested` | resets local test reminder | remove | use evidence records: `atelier evidence add --kind validation --result pass ...` and attach them to the validated issue or mission | `atelier-drfm` |

Review against closed decisions:
- Matches `atelier-9jbu`: lifecycle keep set is create/list/show/update/close; quick/subissue/reopen/label/unlabel/blocked fold into lifecycle commands; block/unblock, relation, search, comment, impact/tree, delete move out; close-all, next, and tested are removed unless a later maintenance issue proves a retained path.
- Matches `atelier-o2a4`: command homes use domain groups, old command shapes are not preserved by default, and the normal issue surface stays lifecycle-only.

No new follow-up issue is needed from this inventory. The two discovered folded replacement gaps are already in scope for `atelier-64ea`; the missing moved command groups and removed-command behavior are already in scope for `atelier-drfm`.
